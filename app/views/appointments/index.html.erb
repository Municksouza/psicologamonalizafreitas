# <h1>Appointment Calendar</h1>
# <div id='calendar'></div>

# <script src='https://cdn.jsdelivr.net/npm/@fullcalendar/core@4.4.2/main.min.js'></script>
# <script src='https://cdn.jsdelivr.net/npm/@fullcalendar/daygrid@4.4.2/main.min.js'></script>
# <script>
#   document.addEventListener('DOMContentLoaded', function() {
#     var calendarEl = document.getElementById('calendar');
#     var calendar = new FullCalendar.Calendar(calendarEl, {
#       plugins: [ 'dayGrid' ],
#       events: <%= raw @appointments.to_json %>
#     });
#     calendar.render();
#   });
# </script>

# <!-- app/views/appointments/index.html.erb -->
# <h1>Available Appointments</h1>
# <% if psychologist_signed_in? %>
#   <!-- List appointments created by the psychologist -->
#   <% @appointments.each do |appointment| %>
#     <%= appointment.start_time %> - <%= appointment.end_time %>
#     <%= link_to 'Edit', edit_appointment_path(appointment) %>
#     <%= link_to 'Delete', appointment_path(appointment), method: :delete, data: { confirm: 'Are you sure?' } %>
#   <% end %>
#   <%= link_to 'New Appointment Slot', new_appointment_path %>
# <% elsif patient_signed_in? %>
#   <!-- List available appointment slots for patients to book -->
#   <% @appointments.each do |appointment| %>
#     <% if appointment.patient_id.nil? %> <!-- Show only unbooked slots -->
#       <%= appointment.start_time %> - <%= appointment.end_time %>
#       <%= form_with(model: appointment, url: appointment_path(appointment), method: :patch) do |f| %>
#         <%= hidden_field_tag :appointment_id, appointment.id %>
#         <%= f.submit "Book This Appointment" %>
#       <% end %>
#     <% else %>
#       Booked by: <%= appointment.patient.name %>
#     <% end %>
#   <% end %>
# <% end %>



# <!-- app/views/appointments/index.html.erb -->
# <h1>Available Appointments</h1>

# <!-- Loop through each appointment and display its details -->
# <% @appointments.each do |appointment| %>
#   <p>
#     <%= appointment.start_time.strftime("%B %d, %Y %H:%M") %> - <%= appointment.end_time.strftime("%B %d, %Y %H:%M") %>

#     <!-- Book appointment button for patients -->
#     <% if patient_signed_in? && appointment.status == 'available' %>
#       <%= link_to 'Marque sua Consulta', book_appointment_path(appointment), method: :patch, class: "booking-btn" %>
#     <% end %>

#     <!-- New appointment button for psychologists -->
#     <% if psychologist_signed_in? %>
#       <%= link_to 'Marque uma Consulta', new_appointment_path, class: "booking-btn" %>
#     <% end %>
#   </p>
# <% end %>
