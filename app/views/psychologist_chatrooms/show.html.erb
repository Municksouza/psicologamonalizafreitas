<div class="container chatroom p-5" data-controller="psychologist-chatroom-subscription" data-psychologist-chatroom-subscription-chatroom-id-value="<%= @psychologist_chatroom.id %>">
  <h1># Private Chat</h1>

  <div class="messages" data-psychologist-chatroom-subscription-target="messages">
    <% @psychologist_chatroom.messages.each do |message| %>
      <%= render "messages/message", message: message %>
    <% end %>
  </div>
    <%= simple_form_for [@psychologist_chatroom, @message],
    html: { data: { action: "turbo:submit-end->psychologist-chatroom-subscription#resetForm" }, class: "d-flex" } do |f|
  %>
    <%= f.input :content,
      label: false,
      placeholder: "Message #Private Chat",
      wrapper_html: {class: "flex-grow-1"}
    %>
    <%= f.submit "Send", class: "btn btn-primary mb-3" %>
  <% end %>
</div>
